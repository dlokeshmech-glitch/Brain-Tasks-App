version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Build started
      - docker build -t brain-app .

  post_build:
    commands:
      - echo Build completed
